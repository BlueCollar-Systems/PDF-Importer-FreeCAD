# Legacy FC PDF core mirror (archived)

Duplicate extraction/recognition modules that mirrored `pdfcadcore/` before D6
quarantine (2026-06-09). `PDFImporterCore.py` remains the FreeCAD host adapter.

`src/PDF*.py` (except host-specific tools) are DeprecationWarning shims to
`pdfcadcore`. Originals kept here for reference.
